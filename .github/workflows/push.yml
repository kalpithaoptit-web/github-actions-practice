name: My First GitHub Action

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "👋 Triggered by: ${{ github.event_name }}"
